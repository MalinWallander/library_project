package com.library.db;

import com.library.model.Employee;

public interface EmployeeDao {
    // TODO: Return value is never used, use void instead?
    Employee createEmployee(Employee employee);
}
