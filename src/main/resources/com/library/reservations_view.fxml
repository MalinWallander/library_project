<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox spacing="20" alignment="CENTER"
      xmlns="http://javafx.com/javafx/8"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.library.view.user.ReservationController"
      prefWidth="350" prefHeight="300"
      styleClass="root-pane"
      stylesheets="@main.css"
      style="-fx-padding: 25;">

    <Label text="Bekräfta reservation" styleClass="page-title" />

    <VBox spacing="12" styleClass="info-container">
        <VBox>
            <Label text="TITEL" styleClass="label-hint" />
            <Label fx:id="titleLabel" styleClass="card-title" />
        </VBox>
        
        <VBox>
            <Label text="SKAPARE" styleClass="label-hint" />
            <Label fx:id="creatorLabel" styleClass="card-title" />
        </VBox>
    </VBox>

    <HBox spacing="15" alignment="CENTER">
        <Button text="Avbryt" onAction="#handleCancel" styleClass="btn-secondary" />
        <Button text="Bekräfta" onAction="#handleConfirmReservation" styleClass="btn-primary" />
    </HBox>

</VBox>